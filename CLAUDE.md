# CLAUDE.md — yoyo-player-map

Read `AGENTS.md` first — same orientation content, tool-agnostic. This file exists only
because Claude Code auto-loads `CLAUDE.md` specifically; don't duplicate content here, it will
drift out of sync with `AGENTS.md`.

Also read `.agents/AGENTS.md` — a broader cross-repo maintainer doc covering this repo,
`dmvthrowers.github.io`, `dmvt-event-hub`, and `DMVT-Design` together (brand voice, color/
typography system, cross-repo workflows).
